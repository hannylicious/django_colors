"""App configuration for the colors app."""

from django.apps import AppConfig


class ColorsConfig(AppConfig):
    """Configuration class for the colors app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "colors"
